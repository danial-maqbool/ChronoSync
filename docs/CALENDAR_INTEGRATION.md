# Calendar integration

Manual approval is the default. Tests use a deterministic mock provider. Outlook COM availability must be checked explicitly; ICS export is a fallback and must never be reported as direct calendar synchronization. Real calendar deletion requires a separate user choice.
