# Architecture

Target: Python FastAPI, SQLAlchemy, Alembic and SQLite backend; React and TypeScript frontend. Ingestion retains segments and source timestamps. Deterministic temporal reasoning produces reviewable candidates. Calendar providers are isolated from extraction.
